# 发布 AIFlow 到 npm 的步骤

完成配置后，按照以下步骤发布包：

## 1. 安装依赖

```bash
npm install
```

## 2. 构建库

```bash
npm run build:lib
```

## 3. 登录 npm

如果还没有登录 npm，需要先登录：

```bash
npm login
```

按提示输入用户名、密码和邮箱。

## 4. 发布包

```bash
npm publish
```

如果是第一次发布带有 scope 的包，需要添加 `--access=public` 参数：

```bash
npm publish --access=public
```

## 5. 发布新版本

修改 `package.json` 中的 `version` 字段，然后重新构建并发布：

```bash
# 更新版本号 (patch, minor, or major)
npm version patch

# 构建
npm run build:lib

# 发布
npm publish
```

## 注意事项

1. 确保 `package.json` 中的 `name` 字段是唯一的，最好使用你的 npm 用户名作为 scope
2. 发布前检查 `.npmignore` 和 `package.json` 的 `files` 字段，确保只包含必要的文件
3. 使用 `npm pack` 命令可以在发布前预览将被发布的文件内容
4. 确保修改 README.md 中的安装说明，使其与实际包名一致 