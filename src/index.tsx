import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// 在React 17中使用不带StrictMode的渲染方式
ReactDOM.render(
  <App />,
  document.getElementById('root')
);